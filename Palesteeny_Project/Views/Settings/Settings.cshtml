@model Palesteeny_Project.Models.UserPal
@{
    ViewData["Title"] = "الإعدادات والخصوصية";
    Layout = "_Layout";
}
<link rel="stylesheet" href="~/css/Settings.css" />
<div class="settings-title">
    <a asp-action="Settings" asp-controller="Settings" class="settings-title-wr">الإعدادات والخصوصية</a>
    <a asp-action="Settings" asp-controller="Settings"><img src="~/images/left-arrow.png" width="54.5px" height="54.5px"></a>
</div>
<div class="settings-container">
     <div class="privacy-section">
        <p>نحن نهتم بخصوصيتك! يمكنك التحكم في حسابك من هنا، بما في ذلك حذف الحساب إذا رغبت.</p>
        <p>إذا قررت حذف حسابك، سيتم إزالة جميع بياناتك من منصتنا بشكل دائم.</p>
    </div>

    <div class="user-info-box">
        <p><strong>الاسم:</strong> @Model.FirstName @Model.LastName</p>
        <p><strong>البريد الإلكتروني:</strong> @Model.Email</p>
        <p><strong>الجنس:</strong> @Model.Gender</p>
    </div>

    <div class="delete-account-box">
        <button class="btn-delete" onclick="showBobobConfirm()">حذف الحساب🗑️ </button>
    </div>
</div>

<!-- Bobob confirmation modal -->
<div id="bobobModal" class="modal-overlay">
    <div class="modal-box">
        <p>هل أنت متأكد أنك تريد حذف حسابك؟ 😢</p>
        <form method="post" asp-action="DeleteAccount">
            <button type="submit" class="btn-confirm">نعم، احذف الحساب</button>
            <button type="button" class="btn-cancel" onclick="hideBobobConfirm()">إلغاء</button>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        function showBobobConfirm() {
            document.getElementById('bobobModal').style.display = 'flex';
        }
        function hideBobobConfirm() {
            document.getElementById('bobobModal').style.display = 'none';
        }
    </script>
}
